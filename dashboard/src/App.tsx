export default function App() {
  return (
    <div className="min-h-screen bg-background">
      <header className="h-12 border-b border-border flex items-center justify-between px-6 bg-surface">
        <span className="font-semibold text-base text-text">Interlock</span>
        <span className="text-text-muted text-xs">Loading...</span>
      </header>
      <main className="max-w-7xl mx-auto px-6 py-6">
        <p className="text-text-muted">Dashboard loading...</p>
      </main>
    </div>
  );
}
